## 技术规范：物理映射要求 (Physical Mapping Requirements)

### 1. 逻辑状态定义 (Logic State Definition)
任何适配 SPL-G1 的材料载体，必须定义两种及以上的离散稳定物理状态 $\sigma_0$ 和 $\sigma_1$。
- **转换能量限制**：从 $\sigma_0 \rightarrow \sigma_1$ 的激活能必须大于热涨落噪声 10 倍以上，以确保“叙事噪声”不会引发自发逻辑翻转。

### 2. 因果路径传导 (Causal Path Routing)
材料必须支持**定向信号矢量**。
- 在光基载体中，通过非对称波导实现。
- 在生物载体中，通过酶促反应的定向催化实现。
- **禁止项**：禁止使用具有全向各向同性扩散特征的材料作为 RA-BUS（因果总线）的载体。

### 3. PIM (存算一体) 物理近接准则
适配材料必须允许执行单元（Logic Gate）与存储单元（Storage Bit）在纳米尺度上实现三维堆叠或原子级重合。
- **功耗阈值**：单次逻辑转换引发的数据移动功耗必须趋近于 0（$\Delta E_{transport} \approx 0$）。

### 4. SBC (安全边界) 硬件效力
硬件网关必须利用材料的**不可逆物理特性**（例如：单向导电、量子隧穿阈值、生物半透膜）实现隔离。
- **攻击防御**：安全边界的破坏必须导致材料物理结构的永久性损坏，使芯片进入逻辑锁定（Logic Lock）状态。

---
**Hash Anchor Reference**: `59a4ca22-Universal-Clock-Sync`
